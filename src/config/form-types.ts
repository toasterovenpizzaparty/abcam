/**
 *
 * @description @Deprecated a list of names for our steps.
 */
export enum STEPS {
  STEP_ONE = "step-one",
  STEP_TWO = "step-two",
}

/**
 *
 * @description A set list of field-keys we will be using to store our field values.
 */
export enum FIELDS {
  RATING = "rating",
  TITLE = "title",
  DESCRIPTION = "description",
  IMAGE = "image",
  IMAGE_DESCRIPTION = "image-description",
}
